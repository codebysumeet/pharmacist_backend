PORT=5000
# Replace the URI below with your actual MongoDB Atlas connection string to host it in the cloud.
# Make sure to replace <username> and <password> with your Atlas database user details.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/pharmacist?retryWrites=true&w=majority
